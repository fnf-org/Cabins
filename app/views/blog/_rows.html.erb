<% @posts.each do |p| %>
  <div class="row">
    <div class="col border rounded p-3 mt-2 mb-2 bg-white">
      <h5 class="card-title"><%= p.title.try(:html_safe) %></h5>
      <div><%= p.body.try(:html_safe) %></div>
    </div>
  </div>
<% end %>
