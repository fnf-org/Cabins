<!DOCTYPE html>
<html lang="en">
<head>
  <title>Cabins</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <%= yield :head %>
  <link rel="apple-touch-icon" sizes="180x180" href="<%= image_url('apple-touch-icon.png') %>">
  <link rel="icon" type="image/png" sizes="32x32" href="<%= image_url('favicon-32x32.png') %>">
  <link rel="icon" type="image/png" sizes="16x16" href="<%= image_url('favicon-16x16.png') %>">

  <link rel="manifest" href="/site.webmanifest">
</head>
<body>
  <div class="container">
<%= render 'layouts/logo' %>
<%= render 'layouts/header' %>

    <% if !flash.empty? %>
      <% flash.each do |message_type, message| %>
          <div class="alert alert-<%= message_type %> mt-3" role="alert"><%= message %></div>
      <% end %>
    <% end %>

<%= yield %>

<%= render 'layouts/footer' %>
  </div><!-- end container -->
  <%= debug(params) if Rails.env.development? %>
</body>
</html>
